# Contributing to Splendor

Thanks for helping build Splendor. Please follow these basics rules:

- Format Rust with `cargo fmt` and address `cargo clippy` warnings.
- Run `cargo test --workspace` and `pytest python/tests` before submitting changes.
- Keep new Rust code in the appropriate crate under `crates/`.
- Keep Python SDK changes under `python/splendor/`.
